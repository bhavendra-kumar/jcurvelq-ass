import { Search, MessageSquare, Code, FileText, Database } from 'lucide-react';
import React from 'react';

export const iconMap: Record<string, React.ReactNode> = {
  Search: <Search />,
  MessageSquare: <MessageSquare />,
  Code: <Code />,
  FileText: <FileText />,
  Database: <Database />,
};
